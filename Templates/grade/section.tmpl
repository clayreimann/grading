<%inherit file="../base.html" />
<%block name="title">
  <title>${page_title}</title>
</%block>

<h2>Students in ${section}</h2>
<ul>
  % for student in students:
    <li><a href="${section}/${student}">${student}</a></li>
  % endfor
</ul>
